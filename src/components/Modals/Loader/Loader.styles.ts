import styled from 'styled-components';
import { ModalContent } from '../Modal/Modal';

const ModalContentLoader = styled(ModalContent)`
  background-color: transparent;
  border: none;
`;

export default ModalContentLoader;
