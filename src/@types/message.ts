export enum ErrorMessage {
  NoService = 'Unavailable service! Please try again.',
  NoTeamsProvider = 'useTeams must be used within a TeamsProvider.',
  NoToastProvider = 'useToast must be used within a ToastProvider.',
  NoLoaderProvider = 'useLoader must be used within a LoaderProvider.',
}

export enum SuccessMessage {
  registerOk = 'Team successfully registered!',
  updateOk = 'Team updated successfully!',
}
